# ADR-0001：保留三个独立仓库

- 状态：已采纳
- 日期：2026-07-19

## 背景

主仓库曾只把 Android 作为子模块，Windows 却保持独立。这个关系不对称，子模块指针也容易落后于平台仓库。

## 决定

主仓库、Android 和 Windows 继续独立维护，但不再使用 Git submodule。主仓库负责公共入口和 Release，平台仓库负责可独立构建的源码。

## 结果

跨平台修改需要多个提交，发布记录必须写明各平台提交。换来的好处是克隆、权限和 CI 边界清楚，也不会让一个过期 gitlink 冒充当前平台版本。
