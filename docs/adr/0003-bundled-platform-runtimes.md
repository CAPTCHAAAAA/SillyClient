# ADR-0003：应用自带平台运行时

- 状态：已采纳
- 日期：2026-07-19

## 背景

依赖 Termux 或用户的系统 Node.js 会让安装结果受外部环境影响，也会让控制台提示与真实执行方式不一致。

## 决定

Android 打包 arm64 Bionic Node.js，Windows 打包官方 Windows x64 Node.js。平台层只使用随应用分发的运行时。

## 结果

安装包更大，但首次使用更可预测。构建流程必须固定运行时版本、校验下载内容，并在发布前用实际安装包验证。
