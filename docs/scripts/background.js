/*! SillyClient background entry: Color Bends + Dot Field. */
(() => {
  import('./background/index.js?v=20260726-background-modules-v1').catch(error => {
    console.error('Background renderer failed to load:', error);
  });
})();
